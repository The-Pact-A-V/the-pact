import StubScreen from '@/components/StubScreen'

export default function History() {
  return <StubScreen eyebrow="history" title="The whole pact" back="/settings" note="Calendar · per-habit stats · all past weeks — soon." />
}
