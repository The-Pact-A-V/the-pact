import StubScreen from '@/components/StubScreen'

export default function PactNew() {
  return <StubScreen eyebrow="create" title="A new pact" back="/onboarding" note="Name · window · target · reward — soon." />
}
