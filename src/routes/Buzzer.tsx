import StubScreen from '@/components/StubScreen'

export default function Buzzer() {
  return <StubScreen eyebrow="buzz" title="Send a buzz" back="/dashboard" note="Preset emoji or a custom message — soon." />
}
